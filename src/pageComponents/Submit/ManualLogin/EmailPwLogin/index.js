import React from "react"
import BigBtn from "../../../../reusableComponents/BigBtn"

const EmailPwLogin = ({ action }) => {
  return <BigBtn text={`Netfangi & Lykilorði`} action={action} button></BigBtn>
}

export default EmailPwLogin
