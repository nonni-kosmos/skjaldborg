import React from "react"
import { Container, Image } from "./styled"

const Topimage = ({ image }) => {
  return <Container>{/* <Image fluid={image.fluid}></Image> */}</Container>
}

export default Topimage
